- name: Declare variable at Play level
  hosts: all
  connection: local
  vars:
    abc: example.play.com
  tasks:
    - name: access a variable
      ansible.builtin.debug:
        msg: xyz == {{abc}}