- name:  Defined variable in another file and accessing at task level
  hosts:  all
  connection:  local
  tasks:
    - name:  Defined variable in another file and accessing at task level
      ansible.builtin.debug:
        msg:  SSS == {{fff}}
      vars_files:
        - file1.yml